rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Exercise SVGs are publicly readable
    match /exercises/{slug} {
      allow read: if true;
      allow write: if false; // server SDK only
    }
  }
}
